# ignore memfs/memfs, because this is FUSE
memfs/memfs

# ignore .o files which is generated by Makefile
memfs/*.o

# ignore memfs/memfs.c because this is old file whic I already seperated to multi files
memfs/memfs.c

